const pages = document.querySelectorAll('.page'); const nav = document.querySelectorAll('.nav-item[data-page]'); const title = document.querySelector('#pageTitle'); const titles={chats:'Good evening, Alex',anonymous:'Anonymous',gifts:'Gift store',profile:'Profile',creator:'Creator workspace'};
nav.forEach(button=>button.addEventListener('click',()=>{nav.forEach(n=>n.classList.remove('active'));button.classList.add('active');pages.forEach(p=>p.classList.toggle('active-page',p.id===button.dataset.page));title.textContent=titles[button.dataset.page]}));
const toast=document.querySelector('.toast'); const show=m=>{toast.textContent=m;toast.classList.add('show');setTimeout(()=>toast.classList.remove('show'),2600)}; document.querySelector('#privacyButton').addEventListener('click',e=>{e.currentTarget.classList.toggle('enabled');show(e.currentTarget.classList.contains('enabled')?'Streamer mode enabled locally':'Streamer mode disabled')}); document.querySelector('#newChat').addEventListener('click',()=>show('New chat creation is ready to connect to Chat Service.')); document.querySelector('#composer').addEventListener('submit',e=>{e.preventDefault();const i=e.currentTarget.querySelector('input');if(i.value.trim()){show('Message queued for secure delivery.');i.value=''}});
